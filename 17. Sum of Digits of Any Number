#include<stdio.h>
#include<conio.h>
int main()
{
	int a,b,sum=0;
	printf("Enter any no.:");
	scanf("%d",&a);
	while(a!=0)
	{
		b=a%10;
		a=a/10;
		sum=sum+b;
	}
	printf("the sum of the digits is:%d ",sum);
	getch();
	return 0;
	
}
