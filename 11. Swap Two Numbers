#include<stdio.h>
#include<conio.h>
int main()
{
    int a,b,temp;
    printf("enter the value of a and b:");
    scanf("%d%d",&a,&b);
    temp=a;
    a=b;
    b=temp;
    printf("a is %d \n b is %d",a,b);
 	getch();
	return 0;	
	
}
