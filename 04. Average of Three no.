#include<stdio.h>
#include<conio.h>
int main()
{
	int a,b,c;
	float average;
	printf("enter the value of a,b and c:");
	scanf("%d%d%d",&a,&b,&c);
	average=(a+b+c)/3;
	printf("average of the three no. is: %f",average);
	getch();
	return 0;	
	
}
