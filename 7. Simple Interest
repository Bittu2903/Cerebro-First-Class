#include<stdio.h>
#include<conio.h>
int main()
{
	int p,t;
	float r,SI;
	
	printf("enter the value principle,rate,time:");
	scanf("%d%f%d",&p,&r,&t);
    SI=(p*r*t)/100;
	printf("Simple Interset is %f",SI);
	
	getch();
	return 0;
	
}
